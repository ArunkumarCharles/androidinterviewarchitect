# Add project specific R8 rules here.
# AGP will combine all keep rule files in src/main/keepRules to pass to R8
#
# For more details, see
#   https://d.android.com/r/tools/r8/keep-rules

# If your project uses WebView with JS, uncomment the following
# and specify the fully qualified class name to the JavaScript interface
# class:
#-keepclassmembers class fqcn.of.javascript.interface.for.webview {
#   public *;
#}

# Network DTOs are deserialized reflectively-by-name via kotlinx.serialization generated serializers;
# the library ships its own consumer rules, so only keep our model package's serializer companions.
-keepclassmembers class com.sevvanam.android_interview_architect.core.model.** {
    *** Companion;
}
-keepclasseswithmembers class com.sevvanam.android_interview_architect.core.model.** {
    kotlinx.serialization.KSerializer serializer(...);
}
