<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>LongMethod:CheckoutScreen.kt$@OptIn(ExperimentalMaterial3Api::class) @Composable fun CheckoutRoute( viewModel: CheckoutViewModel = hiltViewModel() )</ID>
    <ID>LongMethod:FeedScreen.kt$@OptIn(ExperimentalMaterial3Api::class) @Composable fun FeedContent( uiState: FeedUiState, onIntent: (FeedIntent) -&gt; Unit, snackbarHostState: SnackbarHostState = remember { SnackbarHostState() }, // When provided, the list is rendered from Paging (Room-backed, loaded in pages). Loading/Error // still come from uiState. Null keeps the simple list path, which previews and UI tests use. pagedPosts: LazyPagingItems&lt;Post&gt;? = null )</ID>
    <ID>MagicNumber:CacheSyncWorker.kt$CacheSyncWorker$3</ID>
    <ID>MagicNumber:CacheSyncWorker.kt$CacheSyncWorker.Companion$6</ID>
    <ID>MagicNumber:CheckoutRepositoryImpl.kt$CheckoutRepositoryImpl$1500</ID>
    <ID>MagicNumber:SeedData.kt$100_000L</ID>
    <ID>MatchingDeclarationName:AppRoutes.kt$AppRoute</ID>
    <ID>MaxLineLength:ProfileScreen.kt$Text(text = "Email: ${currentProfile.email}", style = MaterialTheme.typography.bodyMedium, modifier = Modifier.padding(top = 4.dp))</ID>
    <ID>MaxLineLength:SeedData.kt$"@Binds maps an interface to an implementation with no generated body, so it is cheaper and preferred. @Provides is for third-party or builder-created objects you cannot constructor-inject."</ID>
    <ID>MaxLineLength:SeedData.kt$"@Singleton for app-wide expensive objects (database, Retrofit, OkHttp). @ViewModelScoped for objects that should live and die with one ViewModel, such as per-screen use cases holding state."</ID>
    <ID>MaxLineLength:SeedData.kt$"A Baseline Profile ships pre-compiled hot code paths with your APK, cutting cold-start time and first-frame jank by up to ~30%. Generate it with Macrobenchmark and measure before and after — never guess at performance."</ID>
    <ID>MaxLineLength:SeedData.kt$"A ViewModel survives rotation but not the OS killing your process in the background. SavedStateHandle persists small, serializable UI state (ids, filters, scroll keys) so the user returns to where they were. Keep big data in Room, not in the handle."</ID>
    <ID>MaxLineLength:SeedData.kt$"Annotate it @HiltWorker with @AssistedInject, and have the Application implement Configuration.Provider returning a HiltWorkerFactory."</ID>
    <ID>MaxLineLength:SeedData.kt$"At each layer boundary: data mappers convert entities/DTOs to domain models; the presentation layer maps domain to UI state. This stops schema or API changes from leaking into upper layers."</ID>
    <ID>MaxLineLength:SeedData.kt$"Base: many JVM unit tests for logic. Middle: integration tests such as in-memory Room and repositories. Top: a few UI/end-to-end tests for critical journeys, since they are slow and flaky."</ID>
    <ID>MaxLineLength:SeedData.kt$"Bump the version, write a Migration with explicit SQL, export schemas to source control, and verify with MigrationTestHelper. Only use destructive fallback for rebuildable caches."</ID>
    <ID>MaxLineLength:SeedData.kt$"Child coroutines are bound to their parent scope, so cancelling a ViewModel's viewModelScope cancels everything it launched. Never swallow CancellationException in a broad catch — rethrow it, or the coroutine will not stop."</ID>
    <ID>MaxLineLength:SeedData.kt$"Compose skips a composable only when all its parameters are stable and unchanged. Unstable types (plain List, classes with var fields) force recomposition. Prefer immutable data classes, kotlinx ImmutableList, or @Immutable, and verify with the compiler metrics report."</ID>
    <ID>MaxLineLength:SeedData.kt$"Dispatchers.Main for UI, IO for blocking network/disk, Default for CPU-heavy work. Inject dispatchers instead of hardcoding them so tests can swap in a TestDispatcher and stay deterministic."</ID>
    <ID>MaxLineLength:SeedData.kt$"Enforced boundaries, parallel and incremental builds, clear team ownership, and the ability to reuse or replace modules. The cost is more configuration, so split by real seams, not prematurely."</ID>
    <ID>MaxLineLength:SeedData.kt$"Expensive shared singletons (DB, Retrofit) get @Singleton; per-screen use cases get @ViewModelScoped so they die with the screen."</ID>
    <ID>MaxLineLength:SeedData.kt$"Fakes give realistic behaviour and survive refactors; mocks verify interactions and are quick for simple collaborators. Prefer fakes for repositories and data sources, mocks for pure interaction checks."</ID>
    <ID>MaxLineLength:SeedData.kt$"Features depend only on core and domain modules. Cross-feature communication goes through the app module's navigation or shared domain interfaces, and a lint/dependency-graph check enforces it in CI."</ID>
    <ID>MaxLineLength:SeedData.kt$"Features expose only a route/destination contract; the app module owns the NavHost and wires them together. Features never depend on each other directly."</ID>
    <ID>MaxLineLength:SeedData.kt$"In a regular Job the exception cancels the parent and siblings. With a SupervisorJob (viewModelScope uses one) siblings survive, but you must still handle the exception in the failing child or a CoroutineExceptionHandler."</ID>
    <ID>MaxLineLength:SeedData.kt$"Inject a TestDispatcher via Dispatchers.setMain, fake or mock the repository, and use Turbine's test { awaitItem() } to assert each emission deterministically."</ID>
    <ID>MaxLineLength:SeedData.kt$"It stops collecting when the lifecycle drops below STARTED, so upstream flows (DB, location) are not kept active in the background, saving battery and work."</ID>
    <ID>MaxLineLength:SeedData.kt$"LaunchedEffect runs a suspend block tied to the composition and restarts when its key changes. DisposableEffect is for setup/cleanup of non-suspending resources. rememberCoroutineScope gives a scope to launch from event callbacks."</ID>
    <ID>MaxLineLength:SeedData.kt$"Long-lived references to Activities/Contexts (static fields, listeners, coroutines outliving lifecycle). Find them with LeakCanary and the Android Studio heap profiler, and scope work to lifecycles."</ID>
    <ID>MaxLineLength:SeedData.kt$"MVVM is simpler and fits CRUD-style screens. MVI adds a single immutable state plus a reducer, which pays off when several event sources (user, network, timers) must be reconciled predictably and replayed in tests."</ID>
    <ID>MaxLineLength:SeedData.kt$"Many fast JVM unit tests (ViewModels, use cases, reducers), fewer integration tests (Room in-memory, repository), and a thin layer of UI tests. Clean Architecture makes the bottom layer cheap because the domain has no Android dependencies."</ID>
    <ID>MaxLineLength:SeedData.kt$"Measure first with Macrobenchmark and Perfetto/System Trace, find work on the main thread in Application.onCreate and first-frame path, defer or lazy-init it, and add a Baseline Profile."</ID>
    <ID>MaxLineLength:SeedData.kt$"Merge instead of blind replace: read existing rows, keep the local fields, and overwrite only server-owned columns, or store local state in a separate table joined at query time."</ID>
    <ID>MaxLineLength:SeedData.kt$"Moving state out of a composable into its caller, exposing the value and an onChange callback. The composable becomes stateless, reusable and easily previewable and testable."</ID>
    <ID>MaxLineLength:SeedData.kt$"Navigate with saveState = true and restoreState = true with launchSingleTop and popUpTo the start destination, so each tab's back stack and ViewModel state are restored."</ID>
    <ID>MaxLineLength:SeedData.kt$"Pure Kotlin business logic runs in fast JVM unit tests, cannot accidentally depend on framework lifecycles, and stays reusable if the UI toolkit or platform changes."</ID>
    <ID>MaxLineLength:SeedData.kt$"Reading a changed State triggers recomposition of the readers. Reduce it by hoisting state, passing stable/immutable parameters, using keys in lazy lists, deferring reads with lambdas, and derivedStateOf for computed values."</ID>
    <ID>MaxLineLength:SeedData.kt$"Room is the single source of truth and RemoteMediator fetches the next page from the network into it, so the list keeps working offline. PagingSource loads only what is visible, keeping memory flat for very long lists."</ID>
    <ID>MaxLineLength:SeedData.kt$"Routes are @Serializable Kotlin types, so arguments are compile-time checked and refactor-safe, replacing error-prone string concatenation and manual argument parsing."</ID>
    <ID>MaxLineLength:SeedData.kt$"Set constraints (NetworkType.CONNECTED, requiresBatteryNotLow), use PeriodicWorkRequest with a sensible interval and exponential backoff, and enqueue as unique work to avoid duplicates."</ID>
    <ID>MaxLineLength:SeedData.kt$"StateFlow always has a current value and conflates, ideal for UI state. SharedFlow has configurable replay and no initial value, suited to one-shot events, though a Channel is often safer for events that must not be missed."</ID>
    <ID>MaxLineLength:SeedData.kt$"StateFlow is hot, conflated, and Kotlin-native — no lifecycle-transformation boilerplate, and it composes naturally with Flow operators."</ID>
    <ID>MaxLineLength:SeedData.kt$"The UI observes a Flow from Room, the single source of truth. The repository fetches from the network in the background and upserts into Room, which re-emits automatically, so the UI never waits on connectivity."</ID>
    <ID>MaxLineLength:SeedData.kt$"Use @TestInstallIn to replace a production module with a fake one, or @UninstallModules with @BindValue. Prefer constructor-injected fakes in plain unit tests and skip Hilt entirely."</ID>
    <ID>MaxLineLength:SeedData.kt$"Use lazy lists with stable keys and contentType, keep item composables cheap, avoid work in composition, page data with Paging 3, and size images properly via Coil."</ID>
    <ID>MaxLineLength:SeedData.kt$"WorkManager is for deferrable work that must run even if the app is closed or the device restarts (sync, uploads). Coroutines are for in-process work; foreground services are for user-visible ongoing tasks."</ID>
    <ID>MaxLineLength:SeedData.kt$"fallbackToDestructiveMigration wipes user data on a version bump. For production, write explicit Migration objects, export schemas, and test them with MigrationTestHelper. Reserve destructive fallback for caches you can rebuild."</ID>
    <ID>MaxLineLength:SeedData.kt$"flatMapLatest cancels the previous inner flow when a new query arrives, so stale network results can never overwrite fresh ones. Pair it with debounce and distinctUntilChanged to cut wasted requests."</ID>
    <ID>MaxLineLength:SeedData.kt$"implementation hides a dependency from consumers so changes do not trigger their recompilation; api exposes it transitively. Default to implementation and use api only when the type appears in your public surface."</ID>
    <ID>MaxLineLength:SeedData.kt$"remember survives recomposition only; rememberSaveable also survives configuration change and process death by writing to the saved-instance-state Bundle. Use it for user input like text fields and expanded state, never for large objects."</ID>
    <ID>NewLineAtEndOfFile:Color.kt$com.sevvanam.android_interview_architect.ui.theme.Color.kt</ID>
    <ID>NewLineAtEndOfFile:Theme.kt$com.sevvanam.android_interview_architect.ui.theme.Theme.kt</ID>
    <ID>NewLineAtEndOfFile:Type.kt$com.sevvanam.android_interview_architect.ui.theme.Type.kt</ID>
    <ID>SwallowedException:CacheSyncWorker.kt$CacheSyncWorker$e: Exception</ID>
    <ID>SwallowedException:FeedViewModel.kt$FeedViewModel$e: Exception</ID>
  </CurrentIssues>
</SmellBaseline>
